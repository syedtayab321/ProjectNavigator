import 'package:flutter/material.dart';

class ManageNewUserRequests extends StatelessWidget {
  const ManageNewUserRequests({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
